[theme]
inherit = "basic"
stylesheets = ["yroots.css"]
sidebars = []
pygments_style = { default = "native" }

[options]
# The site these docs belong to: the landing page and the section anchors the
# header links back to. Relative when the landing page sits above the docs.
landing_url = "../"
repo_url = "https://github.com/tylerjarvis/RootFinding"
# Optional companion project, linked in the footer when set.
sibling_url = ""
sibling_label = ""
